/**
 * AI driver. Implemented in M4.
 *
 * Follows the racing line, derives a target speed from local curvature, and emits
 * the *same* `InputState` a human produces — so any car can be swapped human/AI,
 * and recorded input can be replayed as a benchmark opponent (ARCHITECTURE.md).
 *
 * May import `core` and `track`. Must not import `render`, `input`, or `audio`.
 */

export {}
